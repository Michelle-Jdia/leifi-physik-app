export enum RouteName {
    HOME_INDEX = 'home-index',
    BRANCH_INDEX = 'branch-index',
    BRANCH_ID = 'branch-id',
    TOPIC_ID = 'topic-id',
    TASK_ID = 'task-id',
    COLLECTION_TASK_ID = `collection-${RouteName.TASK_ID}`,
    FAVORITE_TASK_ID = `favorite-${RouteName.TASK_ID}`,
    HISTORY_TASK_ID = `history-${RouteName.TASK_ID}`,
    SEARCH_TASK_ID = `search-${RouteName.TASK_ID}`,
    HOME_TASK_ID = `home-${RouteName.TASK_ID}`,
    ISSUE_ID = 'issue-id',
    COLLECTION_ISSUE_ID = `collection-${RouteName.ISSUE_ID}`,
    FAVORITE_ISSUE_ID = `favorite-${RouteName.ISSUE_ID}`,
    HISTORY_ISSUE_ID = `history-${RouteName.ISSUE_ID}`,
    SEARCH_ISSUE_ID = `search-${RouteName.ISSUE_ID}`,
    HOME_ISSUE_ID = `home-${RouteName.ISSUE_ID}`,
    DOWNLOAD_ID = 'download-id',
    COLLECTION_DOWNLOAD_ID = `collection-${RouteName.DOWNLOAD_ID}`,
    FAVORITE_DOWNLOAD_ID = `favorite-${RouteName.DOWNLOAD_ID}`,
    HISTORY_DOWNLOAD_ID = `history-${RouteName.DOWNLOAD_ID}`,
    SEARCH_DOWNLOAD_ID = `search-${RouteName.DOWNLOAD_ID}`,
    HOME_DOWNLOAD_ID = `home-${RouteName.DOWNLOAD_ID}`,
    COLLECTION_INDEX = 'collection-index',
    COLLECTION_ID = 'collection-id',
    FAVORITE_INDEX = 'favorite-index',
    HISTORY_INDEX = 'history-index',
    SEARCH_INDEX = 'search-index',
    LOGIN_INDEX = 'login',
    SETTINGS_INDEX = 'settings-index',
    DATA_USAGE = 'data-usage',
    LOCAL_STORAGE = 'local-storage',
    DATA_PROTECTION = 'data-protection',
    IMPRINT = 'imprint',
    DEV = 'dev',
    EXAMPLE = 'example',
}
